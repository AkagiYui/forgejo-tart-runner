sucai/
.DS_Store

# Built artifacts
dist/

# Runtime state & secrets (runner registration, tokens, Forgejo data)
runtime/
runtime-b/
dev/data/
dev/.admin-token
dev/.reg-token
*.runner
.runner

# Plan B build workdir (upstream clone)
plan-b/.build/
