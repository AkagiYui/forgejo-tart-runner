sucai/
.DS_Store

# Built artifacts
dist/

# Runtime state & secrets (runner registration, tokens, Forgejo data)
runtime/
forgejo/data/
forgejo/.admin-token
forgejo/.reg-token
*.runner
.runner

# Plan B build workdir (upstream clone)
plan-b/.build/
